task runUnitTests(dependsOn: ['test']) {
    group = 'nyt__unit_tests'
    description = "Running unit tests"
}

task runInstrumentationTests(dependsOn: ['connectedAndroidTest']) {
    group = 'nyt_instrumentations_tests'
    description = "Running instrumentation tests"
}